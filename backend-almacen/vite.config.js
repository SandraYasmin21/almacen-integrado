import { defineConfig } from 'vite';

// Este archivo se mantiene mínimo porque el backend es 100% API.
// El frontend React está en ../frontend-smartlynk/
// Si en el futuro necesitas compilar assets de Laravel (Blade), agrégalos aquí.
export default defineConfig({});
